# --- Jekyll build outputs ---
_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata

# If you build the site into a nested folder (e.g., maths/_site)
maths/_site/

# --- Ruby/Bundler ---
.bundle/
vendor/bundle/
# Keep Gemfile.lock for deterministic builds on GitHub Pages
# Gemfile.lock

# --- Node / Frontend (future-proof) ---
node_modules/
.cache/

# --- Editors & IDEs ---
.idea/
*.iml
.vscode/

# --- OS files ---
.DS_Store
Thumbs.db

# --- Logs & temp ---
*.log
tmp/

# --- User content notes ---
# Keep versioned: maths/assets/pdf/** (BAC PDFs) — no ignore rule here on purpose.
